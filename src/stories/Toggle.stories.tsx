import { Toggle } from "../components/toggle/Toggle";

export default {
  title: "Toggle",
  component: Toggle,
};

const Template = () => {
  return <Toggle />;
};

export const Default = Template.bind({});
