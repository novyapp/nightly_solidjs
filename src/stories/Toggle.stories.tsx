import { Toggle } from "../components/toggle/Toggle";

export default {
  title: "UI/Toggle",
  component: Toggle,
};

const Template = () => {
  return <Toggle />;
};

export const Default = Template.bind({});
